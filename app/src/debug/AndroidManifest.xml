<?xml version="1.0" encoding="utf-8"?>
<!--
    Solo se aplica a builds debug (merge de manifiestos por variante). El HTTP
    cleartext a 10.0.2.2 (emulador) queda scoped aquí; release hereda el valor
    seguro por defecto (usesCleartextTraffic=false) del manifest principal.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
